<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.adem.iconv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_iconv"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ConfiguracaoActivity" />
        <activity android:name=".ListaConveniosActivity" />
        <activity android:name=".AvaliacaoPassoUmActivity" />
        <activity android:name=".AvaliacaoPassoDoisActivity" />
        <activity android:name=".PrestacaoContasActivity"/>
    </application>

</manifest>
